name: 'football_analytics'
version: '1.0.0'

profile: 'football_analytics'

model-paths: ["models"]

models:
  football_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table